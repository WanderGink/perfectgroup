<%= link_to t("continue_shopping"), root_path,
  class: "btn btn-default" %>

<%= link_to t("checkout"), checkout_path, class: "btn btn-info" %>

<% order_items.each do |order_item| %>
  <%= render "orders/order_row", product: order_item.product, 
    order_item: order_item, show_total: true %>
<% end %>
