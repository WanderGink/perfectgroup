<h2><%= t ".title" %></h2>

<%= form_for resource, as: resource_name, url: password_path(resource_name),
  html: {method: :put} do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :reset_password_token %>
  <div class="field">
    <%= f.text_field :username, placeholder: t("fuser") %><br/>
    <%= f.email_field :email, placeholder: t("femail") %><br/>
    <%= f.telephone_field :phone_number, placeholder: t("fphone") %><br/>
    <%= f.password_field :password, placeholder: t("fpass"),
      autofocus: true, autocomplete: "off" %><br/>
    <%= f.password_field :password_confirmation, placeholder: t("fnpass"),
      autocomplete: "off" %><br/>
    <%= f.submit t(".submit") %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
